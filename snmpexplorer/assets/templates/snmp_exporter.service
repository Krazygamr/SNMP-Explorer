[Unit]
Description=Prometheus SNMP Exporter (upstream)
After=network-online.target

[Service]
User=root
Group=root
Type=simple
ExecStart=/usr/local/bin/snmp_exporter --config.file=/etc/snmp_exporter/snmp.yml --web.listen-address=0.0.0.0:9116
Restart=on-failure

[Install]
WantedBy=multi-user.target
